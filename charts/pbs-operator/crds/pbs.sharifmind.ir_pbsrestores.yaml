---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: pbsrestores.pbs.sharifmind.ir
spec:
  group: pbs.sharifmind.ir
  names:
    kind: PBSRestore
    listKind: PBSRestoreList
    plural: pbsrestores
    singular: pbsrestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.snapshotRef
      name: Snapshot
      type: string
    - jsonPath: .status.startedAt
      name: Started
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PBSRestore is the Schema for the pbsrestores API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PBSRestore
            properties:
              dropKinds:
                description: |-
                  dropKinds lists Kinds excluded from BOTH api apply phases (exact Kind
                  names, e.g. ["Secret","Probe"]).
                items:
                  type: string
                type: array
              keepKinds:
                description: |-
                  keepKinds lists Kinds exempt from dropKinds (Keep wins over Drop when
                  both name the same Kind). Keep alone does not restrict — it is an
                  exception list, not a whitelist.
                items:
                  type: string
                type: array
              repoRef:
                description: repoRef is the name of the PBSRepo (cluster-scoped) to
                  restore from.
                type: string
              snapshotRef:
                description: |-
                  snapshotRef is the PBS snapshot to restore, "host/<id>/<ISO8601Z>"
                  (from a PBSBackup's status or `snapshot list`).
                type: string
              targetNamespace:
                description: |-
                  targetNamespace receives the restored API objects and volumes; it is
                  created if missing. Jobs run there (PVC mounts and the api ConfigMap
                  are namespace-local), so in the common case the PBSRestore itself
                  lives in the target namespace.
                type: string
            required:
            - repoRef
            - snapshotRef
            - targetNamespace
            type: object
          status:
            description: status defines the observed state of PBSRestore
            properties:
              completedAt:
                description: completedAt is when the restore reached a terminal phase.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the PBSRestore
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobs:
                description: jobs holds one RestoreJobStatus per launched Job.
                items:
                  description: RestoreJobStatus reports one restore Job.
                  properties:
                    job:
                      description: job is the name of the k8s Job.
                      type: string
                    phase:
                      description: phase is the restore phase the Job belongs to.
                      type: string
                    state:
                      description: state is a raw summary of the Job's latest condition.
                      type: string
                  required:
                  - job
                  - phase
                  - state
                  type: object
                type: array
              phase:
                default: New
                description: phase is the current lifecycle phase of the restore.
                enum:
                - New
                - StagingAPI
                - RestoringVolumes
                - ApplyingWorkloads
                - Completed
                - Failed
                type: string
              startedAt:
                description: startedAt is when the first restore Job was created.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

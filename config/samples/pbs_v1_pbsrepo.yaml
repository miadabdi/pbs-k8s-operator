apiVersion: pbs.pbs.sharifmind.ir/v1
kind: PBSRepo
metadata:
  labels:
    app.kubernetes.io/name: pbs-operator
    app.kubernetes.io/managed-by: kustomize
  name: pbsrepo-sample
spec:
  # TODO(user): Add fields here

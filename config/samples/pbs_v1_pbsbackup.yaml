apiVersion: pbs.pbs.sharifmind.ir/v1
kind: PBSBackup
metadata:
  labels:
    app.kubernetes.io/name: pbs-operator
    app.kubernetes.io/managed-by: kustomize
  name: pbsbackup-sample
spec:
  # TODO(user): Add fields here

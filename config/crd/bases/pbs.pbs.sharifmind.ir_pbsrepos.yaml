---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: pbsrepos.pbs.pbs.sharifmind.ir
spec:
  group: pbs.pbs.sharifmind.ir
  names:
    kind: PBSRepo
    listKind: PBSRepoList
    plural: pbsrepos
    singular: pbsrepo
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: PBSRepo is the Schema for the pbsrepos API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PBSRepo
            properties:
              bootstrapTokenRef:
                description: |-
                  bootstrapTokenRef references a Secret with a token capable of
                  pre-creating the PBS namespace. Absent means skip bootstrap.
                properties:
                  name:
                    description: name of the Secret.
                    type: string
                  namespace:
                    description: namespace of the Secret. Empty means the operator's
                      own namespace.
                    type: string
                required:
                - name
                type: object
              datastore:
                description: datastore is the PBS datastore name to back up into (min
                  length 3, PBS rule).
                minLength: 3
                type: string
              fingerprint:
                description: |-
                  fingerprint is the PBS server certificate fingerprint
                  (sha256, colon-separated hex, uppercase).
                type: string
              host:
                description: host is the hostname or IP of the PBS server, e.g. "192.168.56.10".
                type: string
              namespace:
                description: namespace is the PBS namespace inside the datastore,
                  e.g. "test-ns".
                type: string
              port:
                default: 8007
                description: port is the PBS API port.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              secretRef:
                description: |-
                  secretRef references the Secret holding the client credentials.
                  Expected keys: tokenID, tokenSecret, keyfile (and optionally host, port,
                  datastore, namespace, fingerprint — CR fields win over Secret keys).
                properties:
                  name:
                    description: name of the Secret.
                    type: string
                  namespace:
                    description: namespace of the Secret. Empty means the operator's
                      own namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - datastore
            - fingerprint
            - host
            - namespace
            - secretRef
            type: object
          status:
            description: status defines the observed state of PBSRepo
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the PBSRepo resource.
                  Condition type "Ready"; reasons include "Reachable", "Unreachable",
                  "SecretMissing", etc. (set via meta.SetStatusCondition).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastProbeTime:
                description: lastProbeTime is when the PBS server was last successfully
                  probed.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
